# SMTP
SMTP_HOST=mail.example.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# false — для SMTP с self-signed сертификатом
SMTP_TLS_REJECT_UNAUTHORIZED=false

# SQLite (по умолчанию native/data/hawk-sender.db)
# DATABASE_PATH=./data/hawk-sender.db
